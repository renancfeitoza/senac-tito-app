import React from 'react'

export const FormProfile = ({
    handleChangeProfile,
    loadin,
    stateProfile
} = this.props) =>{
        return (
            <React.Fragment>

            </React.Fragment>
        )
}